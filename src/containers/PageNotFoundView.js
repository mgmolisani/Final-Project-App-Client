import React, {Component} from 'react';
import PropTypes from 'prop-types'

export default class PageNotFoundView
    extends Component {

    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div>
                PageNotFoundView has successfully been created.
            </div>
        );
    }
}